-- [[ меняй эти настройки для оптимизации ]]
getgenv().Optimization = {
    state = true, -- вкл/выкл
    hide_screen = true, -- выключает прорисовку графики, бустит жесточайше фпс (вкл/выкл)
    fps = 10
};

-- Настройка серверхопа
getgenv().ServerHopSettings = {
    min_players = 2, -- минимальное количество игроков в сервере для перехода
    max_players = 10, -- максимальное количество игроков в сервере для перехода (из 50)
    history_timeout = 3600, -- 1 час - время запоминания серверов (в секундах)
    messages_sent = false, -- флаг отправки сообщений (предотвращает повторную отправку)
    fast_mode = true -- режим быстрого серверхопа
};

-- Создаем глобальную переменную для блокировки серверхопа
_G.IS_HOPPING = false

-- Создаем глобальную переменную для хранения серверов
if not _G.VISITED_SERVERS then
    _G.VISITED_SERVERS = {}
end

-- Создаем файл для постоянного хранения посещенных серверов
local history_file = "server_history.json"
local http_service = game:GetService("HttpService");

-- Загружаем историю серверов из файла (если существует)
pcall(function()
    if isfile(history_file) then
        local history_data = readfile(history_file)
        _G.VISITED_SERVERS = http_service:JSONDecode(history_data)
        print("Загружена история посещенных серверов: " .. #_G.VISITED_SERVERS .. " серверов")
    else
        print("Файл истории серверов не найден, создаем новый")
        _G.VISITED_SERVERS = {}
    end
end)

-- КРИТИЧЕСКИ ВАЖНО: Добавляем текущий сервер в список посещенных
_G.VISITED_SERVERS[game.JobId] = os.time()
-- Сохраняем обновленную историю в файл
pcall(function()
    writefile(history_file, http_service:JSONEncode(_G.VISITED_SERVERS))
end)

-- Запоминаем текущий сервер также в отдельной переменной для дополнительной защиты
_G.CURRENT_SERVER = game.JobId
print("Текущий сервер: " .. _G.CURRENT_SERVER .. " добавлен в черный список")

-- Создаем блокировочный файл для текущего сервера
pcall(function()
    writefile("blocked_server_" .. game.JobId .. ".txt", tostring(os.time()))
end)

if getgenv().Optimization.state then
    if setfpscap ~= nil then setfpscap(getgenv().Optimization.fps); end
    UserSettings().GameSettings.MasterVolume = 0;
    settings().Rendering.QualityLevel = "Level01";
    game:GetService("RunService"):Set3dRenderingEnabled(not getgenv().Optimization.hide_screen);
end

local players = game:GetService("Players");
local local_player = players.LocalPlayer;

-- Исправление: добавляю ожидание и проверки на nil
local rifts
local function getRifts()
    local rendered = workspace:FindFirstChild("Rendered")
    if rendered then
        rifts = rendered:FindFirstChild("Rifts")
    end
    return rifts ~= nil
end

-- Пробуем найти рифты с задержкой
repeat
    task.wait(2) -- Ожидание для загрузки необходимых компонентов
    print("Waiting for Rifts to load...")
until getRifts()

-- Система для предотвращения дублирования вебхуков
local cache_file = "rifts_cache.json"
local rifts_cache = {}

-- Функция для очистки кэша
local function cleanCache()
    local current_time = os.time()
    local new_cache = {}
    local removed_count = 0
    
    for rift_id, timestamp in pairs(rifts_cache) do
        if current_time - timestamp < 540 then -- 9 минут (540 секунд)
            new_cache[rift_id] = timestamp
        else
            removed_count = removed_count + 1
        end
    end
    
    rifts_cache = new_cache
    
    -- Сохраняем обновленный кэш
    pcall(function()
        writefile(cache_file, http_service:JSONEncode(rifts_cache))
    end)
    
    print("Cache cleaned: Removed", removed_count, "items")
end

-- Загружаем кэш, если он существует
if pcall(function() readfile(cache_file) end) then
    pcall(function()
        rifts_cache = http_service:JSONDecode(readfile(cache_file))
    end)
    cleanCache() -- Очищаем кэш при загрузке
end

local whitelisted_eggs = { -- теперь тут меняешь вебхуки НА КАЖДОЕ ЯЙЦО/СУНДУК, в luck через запятую в кавычках прописываешь множитель удачи
    ["rainbow-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211950334906499/_fEmj8oGQ8x4v2kUYeweBlpGdoaCoJ6tIs59BfsBUBeApzBax76neHeWmYbxZyDAXU5f",
        role_id = "", -- ID роли как строка
        luck = { "x25" },
        name = "Rainbow Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529484674240593/latest.png",
        color = 3447003 -- Синий цвет
    },
    ["nightmare-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372212037693866014/agJ1yjXiLDXZAuiJISTd4kr3cCuCRsXuxRVmx42YqrIXJ9qpUSe7wpM9U3-MTYblwQsk",
        role_id = "",
        luck = { "x25" },
        name = "Nightmare Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529530258063370/latest.png",
        color = 10027008 -- Темно-синий цвет
    },
    ["void-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211864133701642/nn8YJOJs5BleUhifO-hA0UNI3nIqnzBlHrhFSTOiibxQBGBjjvMTL8bY9jTciCbjUlNL",
        role_id = "",
        luck = { "x25" },
        name = "Void Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529558158573609/latest.png",
        color = 3355443 -- Темно-серый цвет
    },
    ["royal-chest"] = {
        webhook = "https://discord.com/api/webhooks/1372212119629463753/oVq84wDoSWkyoraVjkYsdsxTBRx1bL3Ui1vUv8eAN7FJNb0Fil27LCOEF5BUT-ZP4Fx_",
        role_id = "1371539682713796648",
        name = "Royal Chest",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372530324445073439/latest.png",
        color = 15277667 -- Розовый цвет
    },
    ["bubble-rift"] = {
        webhook = "https://discord.com/api/webhooks/1372211352357179432/7E34K-TjOiIBvKwze6XcknyeMZr73rN77eGJ1iinqCs6noRMvmNIwm3rGH4_OemmtSah",
        role_id = "",
        name = "Bubble Rift",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372535605103427698/latest.png", -- Добавлена фотка
        color = 3447003 -- Голубой цвет
    },
    ["mining-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211772303609896/PinWw2B_ppgV8JsspqCsfFEx8kbK1ozGZEywUFaO5KjTndjm3FvpE_l6V2XO3_XH_amI",
        role_id = "",
        luck = { "x25" },
        name = "Mining Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529638022320148/latest.png",
        color = 7506394 -- Коричневый цвет
    },
    ["cyber-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211676283273336/Xxiew3FwtHoGld268v6yI46y0AlS9rVlKbjDIIPWLAugNRCO-aN_G7MkXd0z9auQksud",
        role_id = "1371540351361482802",
        luck = { "x25" },
        name = "Cyber Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529595622096977/latest.png",
        color = 16777215 -- Белый цвет
    },
    ["dice-rift"] = {
        webhook = "https://discord.com/api/webhooks/1372211539167281244/CcP3FgYKV-sZAMmtrtu4z5J5wJJ-SCGUAfavJSvyOc-L4uciHxvIiVmsnCMnpUabrb2s",
        role_id = "1371539732022169601",
        name = "Dice Rift",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372530256669315123/latest.png",
        color = 1 -- Черный цвет
    },
    ["underworld-1"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 1",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    },
    ["underworld-2"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 2",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    },
    ["underworld-3"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 3",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    }
};

-- Фиксированные сообщения для отправки (только первые 2 - обязательные)
local required_messages = {
    "Underworld Egg is found! ģģ/ astral-market 🔥",
    "Best Pet Shop = ģģ/ astral-market 🔥"
};

-- ИЗМЕНЕНО: Обновленный формат ссылки для прямого присоединения к серверу
local join_link = string.format('https://www.roblox.com/games/start?placeId=16302670534&launchData=%d/%s', game.PlaceId, game.JobId);

local parse_time = function(text)
    text = string.lower(string.gsub(text, "^%s*(.-)%s*$", "%1"));
    local number, unit = string.match(text, "(%d+)%s*(%a+)");
    if number == nil or unit == nil then 
        return 0;
    end
    
    number = tonumber(number);
    
    if string.find(unit, "second") then
        return number;
    elseif string.find(unit, "minute") then
        return number * 60;
    elseif string.find(unit, "hour") then
        return number * 3600;
    else
        return 0;
    end
end

local time_to_discorddate = function(time_)
    return string.format("<t:%d:R>", time_);
end

local request = (syn ~= nil and syn.request) or (http ~= nil and http.request) or (fluxus ~= nil and fluxus.request) or http_request or request;
-- Улучшенная функция notify для отладки пингов
local notify = function(webhook, data)
    local response = request({
        Url = webhook, 
        Body = http_service:JSONEncode(data), 
        Method = "POST", 
        Headers = { ['content-type'] = "application/json" }
    });
    print("Discord webhook response:", response.StatusCode, response.StatusMessage);
    return response;
end

-- РАДИКАЛЬНО ПЕРЕРАБОТАННАЯ ФУНКЦИЯ СЕРВЕРХОПА БЕЗ ПОВТОРНЫХ ЗАХОДОВ
local function serverhop()
    if _G.IS_HOPPING then
        print("Серверхоп уже выполняется, пропускаем...")
        return
    end
    
    _G.IS_HOPPING = true
    
    print("⚠️ ВЫПОЛНЯЕТСЯ СЕРВЕРХОП ⚠️ " .. os.date("%H:%M:%S"))
    print("Текущий сервер: " .. game.JobId)
    
    -- КРИТИЧЕСКАЯ ОШИБКА БЫЛА ЗДЕСЬ - "Random" не поддерживается API Roblox!
    -- Меняем на корректные значения
    local sort_methods = {"Asc", "Desc"} 
    
    -- Создаем уникальный файл для текущего сервера, чтобы точно не вернуться
    local block_file = "blocked_server_" .. game.JobId .. ".txt"
    pcall(function()
        writefile(block_file, tostring(os.time()))
    end)
    
    -- Принудительно добавляем текущий сервер в черный список на 24 часа
    _G.VISITED_SERVERS[game.JobId] = os.time() + 86400
    
    -- Получаем все серверы
    local all_servers = {}
    
    -- Получаем серверы разными способами
    for index, sort_order in ipairs(sort_methods) do
        if #all_servers >= 50 then break end
        
        -- НОВЫЙ КОД: Добавляем разные параметры для запросов API
        local api_url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=" .. sort_order .. "&limit=100"
        
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet(api_url))
        end)
        
        -- Проверяем, не пустой ли результат
        if success and result and result.data then
            for _, server in ipairs(result.data) do
                -- СТРОГАЯ ПРОВЕРКА: сервер не должен быть текущим и должен иметь свободные места
                if server.id ~= game.JobId and server.maxPlayers > server.playing then
                    -- Проверяем, нет ли файла блокировки для этого сервера
                    local is_blocked = pcall(function() 
                        return isfile("blocked_server_" .. server.id .. ".txt") 
                    end)
                    
                    if not is_blocked and not _G.VISITED_SERVERS[server.id] then
                        table.insert(all_servers, {
                            id = server.id,
                            players = server.playing,
                            maxPlayers = server.maxPlayers
                        })
                    end
                end
            end
        end
        
        -- Добавляем случайную задержку между запросами
        task.wait(math.random() * 0.5)
    end
    
    -- Если не нашли серверов, используем прямой API запрос с обходом кэша
    if #all_servers == 0 then
        print("⚠️ Не найдено подходящих серверов! Используем альтернативный запрос!")
        
        -- Добавляем случайное значение для обхода кэша
        local cache_buster = tostring(math.random(1000000, 9999999))
        local api_url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100&cacheBuster=" .. cache_buster
        
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet(api_url))
        end)
        
        if success and result and result.data then
            for _, server in ipairs(result.data) do
                -- Исключаем ТОЛЬКО текущий сервер
                if server.id ~= game.JobId then
                    table.insert(all_servers, {
                        id = server.id,
                        players = server.playing,
                        maxPlayers = server.maxPlayers
                    })
                end
            end
        end
    end
    
    -- Если все еще нет серверов, запрашиваем с разными параметрами
    if #all_servers == 0 then
        print("⚠️⚠️ Критическая ошибка! Используем экстренный запрос!")
        
        -- Используем другие параметры
        local api_url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?excludeFullGames=true&limit=100"
        
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet(api_url))
        end)
        
        if success and result and result.data then
            for _, server in ipairs(result.data) do
                if server.id ~= game.JobId then
                    table.insert(all_servers, server)
                end
            end
        end
    end
    
    -- Если серверов все равно нет, используем аварийный метод
    if #all_servers == 0 then
        print("⚠️⚠️⚠️ КРИТИЧЕСКАЯ ОШИБКА! Используем случайный телепорт!")
        _G.IS_HOPPING = false
        
        -- Добавляем случайную задержку и используем прямой телепорт
        task.wait(math.random() * 1.5)
        pcall(function()
            game:GetService("TeleportService"):Teleport(game.PlaceId)
        end)
        return
    end
    
    -- Радикальное перемешивание списка серверов
    for i = #all_servers, 2, -1 do
        local j = math.random(i)
        all_servers[i], all_servers[j] = all_servers[j], all_servers[i]
    end
    
    -- Выбираем случайный сервер из первых 10 (или меньше, если их меньше)
    local max_index = math.min(10, #all_servers)
    local chosen_index = math.random(1, max_index)
    local chosen_server = all_servers[chosen_index]
    
    -- ФИНАЛЬНАЯ ПРОВЕРКА: убеждаемся, что не выбрали текущий сервер
    if chosen_server.id == game.JobId then
        -- Если каким-то образом выбрали текущий сервер, выбираем другой
        for i, server in ipairs(all_servers) do
            if server.id ~= game.JobId then
                chosen_server = server
                break
            end
        end
    end
    
    -- Проверяем ещё раз выбранный сервер
    if chosen_server.id == game.JobId then
        print("⚠️⚠️⚠️ КРИТИЧЕСКАЯ ОШИБКА! Выбран текущий сервер, используем прямой телепорт!")
        _G.IS_HOPPING = false
        task.wait(math.random() * 1.5)
        pcall(function()
            game:GetService("TeleportService"):Teleport(game.PlaceId)
        end)
        return
    end
    
    -- Добавляем выбранный сервер в историю
    _G.VISITED_SERVERS[chosen_server.id] = os.time()
    
    -- Сохраняем ID сервера, на который телепортируемся
    pcall(function()
        writefile("last_hop_destination.txt", chosen_server.id)
    end)
    
    print("✅ Выбран сервер: " .. chosen_server.id .. " с " .. chosen_server.players .. " игроками")
    
    -- Добавляем случайную задержку перед телепортацией
    local random_delay = math.random(5, 15) / 10
    print("Задержка перед телепортацией: " .. random_delay .. " секунд")
    task.wait(random_delay)
    
    -- НОВЫЙ КОД: сначала пробуем одним методом, потом другим
    local teleport_attempted = false
    
    -- Первая попытка - через TeleportToPlaceInstance
    pcall(function()
        teleport_attempted = true
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, chosen_server.id, game.Players.LocalPlayer)
    end)
    
    -- Если первая попытка не удалась, делаем задержку и пробуем другой метод
    task.spawn(function()
        task.wait(3)
        if teleport_attempted then
            print("⚠️ Возможно, телепортация не удалась. Пробуем запасной метод...")
            
            -- Второй метод - через ReserveServer
            pcall(function()
                local TeleportService = game:GetService("TeleportService")
                local code = TeleportService:ReserveServer(game.PlaceId)
                TeleportService:TeleportToPrivateServer(game.PlaceId, code, {game.Players.LocalPlayer})
            end)
            
            -- Третий метод - прямой телепорт
            task.wait(2)
            pcall(function()
                game:GetService("TeleportService"):Teleport(game.PlaceId)
            end)
        end
    end)
    
    -- Сбрасываем флаг через 15 секунд на случай сбоя
    task.spawn(function()
        task.wait(15)
        _G.IS_HOPPING = false
    end)
end

task.spawn(function() -- anti afk
    for _,v in pairs(getconnections(local_player.Idled)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocused)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocusReleased)) do v:Disable() end
end)

-- ПРОВЕРКА И ОТПРАВКА ВЕБХУКОВ (ИСПРАВЛЕНО)
-- ВАЖНО: Вынесено в начало для гарантии выполнения
local rifts_found = 0 -- счетчик найденных рифтов
local rifts_sent = 0  -- счетчик отправленных вебхуков

-- Проверка рифтов и отправка вебхуков
for _, rift in pairs(rifts:GetChildren()) do
    if not rift:IsA("Model") or not whitelisted_eggs[rift.Name] then
        continue;
    end
    
    rifts_found = rifts_found + 1
    
    local data = whitelisted_eggs[rift.Name];
    local luck = "None";
    local gui
    
    -- Исправление: добавление проверок на nil и ожидания загрузки
    local display = rift:FindFirstChild("Display")
    if display then
        gui = display:FindFirstChildOfClass("SurfaceGui")
        if data.luck ~= nil then
            if gui == nil then
                repeat 
                    task.wait(0.2)
                    gui = display:FindFirstChildOfClass("SurfaceGui")
                until gui ~= nil or task.wait(2) -- таймаут 2 секунды для быстрой работы
            end
            
            if gui and gui:FindFirstChild("Icon") and gui.Icon:FindFirstChild("Luck") then
                if not table.find(data.luck, gui.Icon.Luck.Text) then
                    continue
                end
                luck = gui.Icon.Luck.Text
            else
                continue -- Пропускаем если нет нужных компонентов
            end
        end
    else
        continue -- Пропускаем если нет Display
    end
    
    if not gui or not gui:FindFirstChild("Timer") then
        continue -- Пропускаем если нет таймера
    end

    -- Создаем уникальный ID для рифта на основе его типа и координат (с округлением)
    local position = rift:GetPivot().Position
    local rift_id = string.format("%s_%d_%d_%d", 
        rift.Name, 
        math.floor(position.X), 
        math.floor(position.Y), 
        math.floor(position.Z))
    
    -- Проверяем, был ли этот рифт уже найден и отправлен ранее
    if rifts_cache[rift_id] then
        print("Rift already reported:", rift_id)
        continue
    end
    
    local time = os.time() + parse_time(gui.Timer.Text);
    local discord_time = time_to_discorddate(time);
    local height = math.floor(rift:GetPivot().Position.Y) - 4;
    
    -- Формируем сообщение для вебхука
    local message = {
        content = data.role_id and data.role_id ~= "" and ("<@&" .. data.role_id .. ">") or "",
        embeds = {
            {
                title = data.name .. " has been found!",
                description = "**[🔗 Click here to join](" .. join_link .. ")**\n\n🪄Tutorial how to join pinned in https://discord.com/channels/1279491047616938005/1369315912049492069",
                fields = {
                    {
                        name = "⏲ Expires in",
                        value = discord_time,
                        inline = true
                    },
                    {
                        name = "📏 Height",
                        value = tostring(height),
                        inline = true
                    },
                    {
                        name = "👤 Players",
                        value = tostring(#players:GetPlayers()),
                        inline = true
                    },
                    {
                        name = "🍀 Luck",
                        value = luck
                    }
                },
                color = data.color or 4508791, -- Используем цвет для рифта или стандартный
                thumbnail = {
                    url = data.image
                },
                footer = {
                    text = "gg/astral-market • " .. os.date("%H:%M", os.time())
                }
            }
        }
    }

    -- Сохраняем рифт в кэше с текущим временем
    rifts_cache[rift_id] = os.time()
    
    -- Сохраняем обновленный кэш
    pcall(function()
        writefile(cache_file, http_service:JSONEncode(rifts_cache))
    end)
    
    -- Отправляем вебхук
    print("Отправка вебхука для: " .. data.name .. " (ID: " .. rift_id .. ")")
    notify(data.webhook, message);
    rifts_sent = rifts_sent + 1
    
    -- Уменьшаем задержку между отправками вебхуков
    task.wait(0.3)
end

print("Rifts found:", rifts_found, "Webhooks sent:", rifts_sent)

-- УСКОРЕННАЯ СИСТЕМА ОТПРАВКИ СООБЩЕНИЙ (ТОЛЬКО ПЕРВЫЕ 2)
task.spawn(function()
    -- Уменьшаем ожидание для более быстрой работы
    task.wait(1)
    
    -- Проверяем, не отправляли ли мы уже сообщения
    if getgenv().ServerHopSettings.messages_sent then
        print("Сообщения уже были отправлены ранее")
        return
    end
    
    local channel
    local success = pcall(function()
        channel = game:GetService("TextChatService"):WaitForChild("TextChannels"):WaitForChild("RBXGeneral", 3)
    end)
    
    if not success or not channel then
        print("Failed to get chat channel, trying alternative method")
        -- Пробуем альтернативный метод для отправки сообщений
        pcall(function()
            local ChatEvents = game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
            if ChatEvents then
                local SayMessageRequest = ChatEvents:FindFirstChild("SayMessageRequest")
                if SayMessageRequest then
                    channel = {
                        SendAsync = function(_, message)
                            SayMessageRequest:FireServer(message, "All")
                            return true
                        end
                    }
                    success = true
                    print("Используем альтернативный метод чата")
                end
            end
        end)
    end
    
    if not success or not channel then
        print("Не удалось инициализировать систему чата, перезаходим без отправки сообщений")
        serverhop()
        return
    end
    
    print("Быстрая отправка 2 обязательных сообщений...")
    local messages_sent = 0
    
    -- Отправляем ТОЛЬКО два обязательных сообщения с УМЕНЬШЕННЫМ интервалом в 1 секунду
    for i, message in ipairs(required_messages) do
        local sent = pcall(function()
            channel:SendAsync(message)
        end)
        
        if sent then
            messages_sent = messages_sent + 1
            print("Отправлено сообщение " .. i .. ": " .. message)
        else
            print("Не удалось отправить сообщение: " .. message)
        end
        
        -- Уменьшаем задержку между сообщениями до 1 секунды
        task.wait(0.5) -- Уменьшено для большей скорости
    end
    
    print("Отправлено всего " .. messages_sent .. " сообщений из 2 запланированных")
    
    -- Устанавливаем флаг, что сообщения уже отправлены
    getgenv().ServerHopSettings.messages_sent = true
    
    -- Запускаем серверхоп СРАЗУ после отправки сообщений
    print("Запуск серверхопа сразу после отправки сообщений...")
    task.wait(0.5) -- Небольшая задержка для стабильности
    serverhop()
end)

-- БЫСТРЫЙ серверхоп через 5 секунд, если еще не выполнен
task.spawn(function()
    task.wait(5) -- Уменьшено с 7 до 5 секунд
    print("Резервный быстрый серверхоп через 5 секунд...")
    serverhop()
end)

-- РЕЗЕРВНЫЙ СЕРВЕРХОП спустя 8 секунд если что-то пошло не так
task.spawn(function()
    task.wait(8) -- Уменьшено с 10 до 8 секунд
    print("Экстренный серверхоп через 8 секунд...")
    
    -- НОВЫЙ КОД: Прямой телепорт с исключением текущего сервера
    pcall(function()
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?limit=100"))
        end)
        
        if success and result and result.data then
            -- Фильтруем только серверы, отличные от текущего
            local valid_servers = {}
            for _, server in ipairs(result.data) do
                if server.id ~= game.JobId and server.id ~= _G.CURRENT_SERVER and server.maxPlayers > server.playing then
                    table.insert(valid_servers, server)
                end
            end
            
            -- Если нашли подходящие серверы, выбираем случайный
            if #valid_servers > 0 then
                local chosen = valid_servers[math.random(1, #valid_servers)]
                print("Экстренная телепортация на сервер: " .. chosen.id)
                game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, chosen.id, game.Players.LocalPlayer)
                return
            end
        end
        
        -- Если не нашли подходящих серверов, используем обычный телепорт
        print("Не найдено подходящих серверов для экстренной телепортации, используем обычный телепорт")
        game:GetService("TeleportService"):Teleport(game.PlaceId)
    end)
end)

-- Обработка кика с сервера
game:GetService("CoreGui"):FindFirstChild("RobloxPromptGui"):FindFirstChild("promptOverlay").ChildAdded:Connect(function(child) -- при кике игрой
    if child.Name == "ErrorPrompt" then
        print("Game kick detected, starting server hop")
        serverhop();
    end
end)

-- Проверка телепортации на тот же сервер
game:GetService("Players").LocalPlayer.OnTeleport:Connect(function(state)
    if state == Enum.TeleportState.Started then
        print("Телепортация начата...")
        -- Сохраняем текущий сервер в глобальной переменной, чтобы точно не вернуться на него
        _G.DEPARTING_SERVER = game.JobId
        pcall(function()
            writefile("last_server.txt", game.JobId)
        end)
    elseif state == Enum.TeleportState.Failed then
        print("Телепортация не удалась, пробуем снова...")
        task.wait(1) -- Задержка для стабильности
        serverhop()
    end
end)

-- НОВАЯ СИСТЕМА ПРОВЕРКИ ПЕРЕЗАХОДА НА ТОТ ЖЕ СЕРВЕР
task.spawn(function()
    task.wait(3) -- Даем время для полной загрузки
    
    -- Проверяем, есть ли файл с последним сервером
    if pcall(function() readfile("last_server.txt") end) then
        local last_server = readfile("last_server.txt")
        if last_server == game.JobId then
            print("⚠️⚠️⚠️ КРИТИЧЕСКАЯ ОШИБКА: ПЕРЕЗАШЛИ НА ТОТ ЖЕ СЕРВЕР!")
            
            -- Добавляем в черный список и сохраняем
            _G.VISITED_SERVERS[game.JobId] = os.time() + 7200 -- Блокируем на 2 часа
            pcall(function()
                writefile(history_file, http_service:JSONEncode(_G.VISITED_SERVERS))
            end)
            
            -- Создаем блокировочный файл, чтобы больше никогда не заходить на этот сервер
            pcall(function()
                writefile("blocked_server_" .. game.JobId .. ".txt", tostring(os.time()))
            end)
            
            -- Удаляем кэш для принудительного обновления
            pcall(function() delfile("last_hop_destination.txt") end)
            
            -- Запускаем немедленный серверхоп с принудительным исключением текущего сервера
            print("Запуск экстренного серверхопа...")
            _G.EMERGENCY_HOP = true
            task.wait(1)
            serverhop()
        else
            print("✅ Успешно подключились к новому серверу")
            -- Обновляем файл последнего сервера
            pcall(function()
                writefile("last_server.txt", game.JobId)
            end)
        end
    else
        -- Создаем файл последнего сервера, если его нет
        pcall(function()
            writefile("last_server.txt", game.JobId)
        end)
    end
end)

-- НОВАЯ СИСТЕМА ПРОВЕРКИ ЗАЦИКЛИВАНИЯ
task.spawn(function()
    task.wait(4)
    
    -- Проверяем, есть ли файл с последним целевым сервером
    if pcall(function() return isfile("last_hop_destination.txt") end) then
        local destination_server = readfile("last_hop_destination.txt")
        
        -- Проверяем, не оказались ли мы на том же сервере
        if destination_server == game.JobId then
            print("⚠️⚠️⚠️ ОБНАРУЖЕНО ЗАЦИКЛИВАНИЕ НА ОДНОМ СЕРВЕРЕ! ПРИНУДИТЕЛЬНЫЙ ВЫЗОВ АЛЬТЕРНАТИВНОГО МЕТОДА!")
            
            -- Удаляем файлы истории для полного сброса
            pcall(function() delfile("server_history.json") end)
            pcall(function() delfile("last_server.txt") end)
            pcall(function() delfile("last_hop_destination.txt") end)
            
            -- Очищаем глобальные переменные
            _G.VISITED_SERVERS = {}
            
            -- Добавляем текущий сервер в черный список
            _G.VISITED_SERVERS[game.JobId] = os.time() + 86400 -- Блокируем на 24 часа
            
            -- Создаем блокировочный файл для текущего сервера
            pcall(function()
                writefile("blocked_server_" .. game.JobId .. ".txt", tostring(os.time()))
            end)
            
            -- Запускаем прямой телепорт с обходом системы серверхопа
            task.wait(math.random() * 2)
            pcall(function()
                print("Принудительная телепортация без выбора конкретного сервера...")
                game:GetService("TeleportService"):Teleport(game.PlaceId)
            end)
        else
            print("✅ Нормальное подключение к новому серверу")
            
            -- Обновляем файл с последним сервером
            pcall(function()
                writefile("last_server.txt", game.JobId)
            end)
        end
    end
end)

-- Выводим информацию в консоль при запуске
print("RiftFinder initialized successfully!")
print("Current server: " .. game.JobId)
print("Server hop system prevents returning to the same server")
print("Быстрый серверхоп через 5 секунд")
