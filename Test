-- [[ меняй эти настройки для оптимизации ]]
getgenv().Optimization = {
    state = true, -- вкл/выкл
    hide_screen = true, -- выключает прорисовку графики, бустит жесточайше фпс (вкл/выкл)
    fps = 10
};

-- Настройка серверхопа
getgenv().ServerHopSettings = {
    min_players = 2, -- минимальное количество игроков в сервере для перехода
    max_players = 10, -- максимальное количество игроков в сервере для перехода (из 50)
    history_timeout = 900, -- 15 минут - время запоминания серверов (в секундах)
    messages_sent = false, -- флаг отправки сообщений (предотвращает повторную отправку)
    hop_attempt = 0, -- счетчик попыток серверхопа
    current_server_id = game.JobId -- сохраняем текущий сервер для двойной проверки
};

-- ГЛОБАЛЬНЫЙ БЛЭКЛИСТ - чтобы избежать перезахода даже между сессиями
if not _G.SERVER_BLACKLIST then
    _G.SERVER_BLACKLIST = {}
end

-- Сразу добавляем текущий сервер в глобальный блэклист
_G.SERVER_BLACKLIST[game.JobId] = true

if getgenv().Optimization.state then
    if setfpscap ~= nil then setfpscap(getgenv().Optimization.fps); end
    UserSettings().GameSettings.MasterVolume = 0;
    settings().Rendering.QualityLevel = "Level01";
    game:GetService("RunService"):Set3dRenderingEnabled(not getgenv().Optimization.hide_screen);
end

local players = game:GetService("Players");
local local_player = players.LocalPlayer;
local http_service = game:GetService("HttpService");

-- Исправление: добавляю ожидание и проверки на nil
local rifts
local function getRifts()
    local rendered = workspace:FindFirstChild("Rendered")
    if rendered then
        rifts = rendered:FindFirstChild("Rifts")
    end
    return rifts ~= nil
end

-- Пробуем найти рифты с задержкой
repeat
    task.wait(2) -- Ожидание для загрузки необходимых компонентов
    print("Waiting for Rifts to load...")
until getRifts()

-- Система для предотвращения дублирования вебхуков
local cache_file = "rifts_cache.json"
local rifts_cache = {}

-- Функция для очистки кэша
local function cleanCache()
    local current_time = os.time()
    local new_cache = {}
    local removed_count = 0
    
    for rift_id, timestamp in pairs(rifts_cache) do
        if current_time - timestamp < 540 then -- 9 минут (540 секунд)
            new_cache[rift_id] = timestamp
        else
            removed_count = removed_count + 1
        end
    end
    
    rifts_cache = new_cache
    
    -- Сохраняем обновленный кэш
    pcall(function()
        writefile(cache_file, http_service:JSONEncode(rifts_cache))
    end)
    
    print("Cache cleaned: Removed", removed_count, "items. Current cache size:", #rifts_cache)
end

-- Загружаем кэш, если он существует
if pcall(function() readfile(cache_file) end) then
    pcall(function()
        rifts_cache = http_service:JSONDecode(readfile(cache_file))
    end)
    cleanCache() -- Очищаем кэш при загрузке
end

-- Запускаем автоматическую очистку кэша каждые 9 минут
task.spawn(function()
    while true do
        task.wait(540) -- 9 минут
        print("Scheduled cache cleanup...")
        cleanCache()
    end
end)

local whitelisted_eggs = { -- теперь тут меняешь вебхуки НА КАЖДОЕ ЯЙЦО/СУНДУК, в luck через запятую в кавычках прописываешь множитель удачи
    ["rainbow-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211950334906499/_fEmj8oGQ8x4v2kUYeweBlpGdoaCoJ6tIs59BfsBUBeApzBax76neHeWmYbxZyDAXU5f",
        role_id = "", -- ID роли как строка
        luck = { "x25" },
        name = "Rainbow Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529484674240593/latest.png",
        color = 3447003 -- Синий цвет
    },
    ["nightmare-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372212037693866014/agJ1yjXiLDXZAuiJISTd4kr3cCuCRsXuxRVmx42YqrIXJ9qpUSe7wpM9U3-MTYblwQsk",
        role_id = "",
        luck = { "x25" },
        name = "Nightmare Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529530258063370/latest.png",
        color = 10027008 -- Темно-синий цвет
    },
    ["void-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211864133701642/nn8YJOJs5BleUhifO-hA0UNI3nIqnzBlHrhFSTOiibxQBGBjjvMTL8bY9jTciCbjUlNL",
        role_id = "",
        luck = { "x25" },
        name = "Void Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529558158573609/latest.png",
        color = 3355443 -- Темно-серый цвет
    },
    ["royal-chest"] = {
        webhook = "https://discord.com/api/webhooks/1372212119629463753/oVq84wDoSWkyoraVjkYsdsxTBRx1bL3Ui1vUv8eAN7FJNb0Fil27LCOEF5BUT-ZP4Fx_",
        role_id = "1371539682713796648",
        name = "Royal Chest",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372530324445073439/latest.png",
        color = 15277667 -- Розовый цвет
    },
    ["bubble-rift"] = {
        webhook = "https://discord.com/api/webhooks/1372211352357179432/7E34K-TjOiIBvKwze6XcknyeMZr73rN77eGJ1iinqCs6noRMvmNIwm3rGH4_OemmtSah",
        role_id = "",
        name = "Bubble Rift",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372535605103427698/latest.png", -- Добавлена фотка
        color = 3447003 -- Голубой цвет
    },
    ["mining-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211772303609896/PinWw2B_ppgV8JsspqCsfFEx8kbK1ozGZEywUFaO5KjTndjm3FvpE_l6V2XO3_XH_amI",
        role_id = "",
        luck = { "x25" },
        name = "Mining Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529638022320148/latest.png",
        color = 7506394 -- Коричневый цвет
    },
    ["cyber-egg"] = {
        webhook = "https://discord.com/api/webhooks/1372211676283273336/Xxiew3FwtHoGld268v6yI46y0AlS9rVlKbjDIIPWLAugNRCO-aN_G7MkXd0z9auQksud",
        role_id = "1371540351361482802",
        luck = { "x25" },
        name = "Cyber Egg",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529595622096977/latest.png",
        color = 16777215 -- Белый цвет
    },
    ["dice-rift"] = {
        webhook = "https://discord.com/api/webhooks/1372211539167281244/CcP3FgYKV-sZAMmtrtu4z5J5wJJ-SCGUAfavJSvyOc-L4uciHxvIiVmsnCMnpUabrb2s",
        role_id = "1371539732022169601",
        name = "Dice Rift",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372530256669315123/latest.png",
        color = 1 -- Черный цвет
    },
    ["underworld-1"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 1",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    },
    ["underworld-2"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 2",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    },
    ["underworld-3"] = {
        webhook = "https://discord.com/api/webhooks/1372273163680874496/vyXpeJcgm6yJFpnIpv9Un-Pt4WOnS8zAmKXk7KuqkCCri8AElQ2LTjx3vZH912KKM7iQ",
        role_id = "1371510979116007545",
        luck = { "x25" },
        name = "Underworld 3",
        image = "https://cdn.discordapp.com/attachments/811315406299725904/1372529424385179789/latest.png",
        color = 16711680 -- Красный цвет
    }
};

-- Фиксированные сообщения для отправки (только первые 2 - обязательные)
local required_messages = {
    "Underworld Egg is found! ģģ/ easyrift 🔥",
    "Best Pet Shop = ģģ/ easyrift 🔥"
};

-- ИЗМЕНЕНО: Обновленный формат ссылки для прямого присоединения к серверу
local join_link = string.format('https://www.roblox.com/games/start?placeId=16302670534&launchData=%d/%s', game.PlaceId, game.JobId);

local parse_time = function(text)
    text = string.lower(string.gsub(text, "^%s*(.-)%s*$", "%1"));
    local number, unit = string.match(text, "(%d+)%s*(%a+)");
    if number == nil or unit == nil then 
        return 0;
    end
    
    number = tonumber(number);
    
    if string.find(unit, "second") then
        return number;
    elseif string.find(unit, "minute") then
        return number * 60;
    elseif string.find(unit, "hour") then
        return number * 3600;
    else
        return 0;
    end
end

local time_to_discorddate = function(time_)
    return string.format("<t:%d:R>", time_);
end

local request = (syn ~= nil and syn.request) or (http ~= nil and http.request) or (fluxus ~= nil and fluxus.request) or http_request or request;
-- Улучшенная функция notify для отладки пингов
local notify = function(webhook, data)
    local response = request({
        Url = webhook, 
        Body = http_service:JSONEncode(data), 
        Method = "POST", 
        Headers = { ['content-type'] = "application/json" }
    });
    print("Discord webhook response:", response.StatusCode, response.StatusMessage);
    return response;
end

-- НОВАЯ УЛУЧШЕННАЯ СИСТЕМА СЕРВЕРХОПА
-- Файл для хранения ГЛОБАЛЬНОЙ истории серверов
local server_history_file = "global_server_history.json"
local visited_servers = {}
local is_server_hopping = false

-- Сохраняем глобальную историю серверов
local function saveServerHistory()
    pcall(function()
        writefile(server_history_file, http_service:JSONEncode(visited_servers))
    end)
end

-- Загружаем глобальную историю серверов
local function loadServerHistory()
    if pcall(function() readfile(server_history_file) end) then
        pcall(function()
            visited_servers = http_service:JSONDecode(readfile(server_history_file))
            -- Очищаем историю от устаревших записей
            local now = os.time()
            local new_history = {}
            for id, time in pairs(visited_servers) do
                if now - time < getgenv().ServerHopSettings.history_timeout then
                    new_history[id] = time
                end
            end
            visited_servers = new_history
        end)
    end
    
    -- Добавляем текущий сервер в историю
    visited_servers[game.JobId] = os.time()
    saveServerHistory()
    
    print("Текущий сервер добавлен в глобальную историю: " .. game.JobId)
    print("Всего в глобальной истории серверов: " .. table.count(visited_servers))
end

-- Функция для проверки, был ли сервер посещен
local function wasServerVisited(server_id)
    -- Многоуровневая проверка
    
    -- 1. Проверка на текущий сервер
    if server_id == game.JobId or server_id == getgenv().ServerHopSettings.current_server_id then
        return true
    end
    
    -- 2. Проверка в глобальном блэклисте
    if _G.SERVER_BLACKLIST[server_id] then
        return true
    end
    
    -- 3. Проверка в истории посещений с учетом времени
    if visited_servers[server_id] then
        local time_since_visit = os.time() - visited_servers[server_id]
        if time_since_visit < getgenv().ServerHopSettings.history_timeout then
            return true
        end
    end
    
    return false
end

-- Загружаем историю при запуске
loadServerHistory()

-- ПОЛНОСТЬЮ ПЕРЕПИСАННАЯ ФУНКЦИЯ СЕРВЕРХОПА
local function serverhop()
    if is_server_hopping then
        print("Серверхоп уже выполняется!")
        return
    end
    
    is_server_hopping = true
    getgenv().ServerHopSettings.hop_attempt = getgenv().ServerHopSettings.hop_attempt + 1
    print("⚠️ ВЫПОЛНЯЕТСЯ СЕРВЕРХОП ⚠️ Попытка #" .. getgenv().ServerHopSettings.hop_attempt .. " | " .. os.date("%H:%M:%S"))
    print("Текущий сервер: " .. game.JobId)
    
    -- Сохраняем ID нашего сервера перед телепортацией
    local current_server = game.JobId
    
    -- Получаем данные о серверах наиболее надежным способом - запрашиваем несколько страниц
    local all_servers = {}
    local pages_checked = 0
    local cursor = ""
    
    repeat
        local api_url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
        if cursor ~= "" then
            api_url = api_url .. "&cursor=" .. cursor
        end
        
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet(api_url))
        end)
        
        if success and result and result.data then
            for _, server in ipairs(result.data) do
                -- Важно: преобразуем ID сервера в строку для единообразия
                local server_id = tostring(server.id)
                
                -- Базовые проверки для подходящего сервера
                if server.playing >= getgenv().ServerHopSettings.min_players and 
                   server.playing <= getgenv().ServerHopSettings.max_players and
                   server.maxPlayers > server.playing then
                    
                    -- РАСШИРЕННАЯ ПРОВЕРКА на уникальность сервера
                    if not wasServerVisited(server_id) then
                        table.insert(all_servers, {
                            id = server_id,
                            players = server.playing,
                            ping = server.ping or 999 -- Берем пинг, если доступен, иначе большое значение
                        })
                    end
                end
            end
            
            -- Получаем курсор для следующей страницы
            cursor = result.nextPageCursor or ""
            pages_checked = pages_checked + 1
            
            print("Страница " .. pages_checked .. ": найдено " .. #result.data .. " серверов, из них подходящих: " .. #all_servers)
        else
            print("Ошибка получения страницы серверов")
            cursor = ""
        end
        
        -- Небольшая задержка между запросами, чтобы не получить бан от Roblox API
        task.wait(0.5)
    until cursor == "" or pages_checked >= 5 or #all_servers >= 20 -- Ограничиваем поиск 5 страницами или 20 подходящими серверами
    
    -- Если после стандартной проверки не нашлось серверов, пробуем с расширенными критериями
    if #all_servers == 0 and getgenv().ServerHopSettings.hop_attempt >= 2 then
        print("Расширяем критерии поиска...")
        
        local api_url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100"
        local success, result = pcall(function()
            return http_service:JSONDecode(game:HttpGet(api_url))
        end)
        
        if success and result and result.data then
            for _, server in ipairs(result.data) do
                local server_id = tostring(server.id)
                
                -- Только проверяем, не текущий ли это сервер и не в блэклисте ли он
                if server_id ~= current_server and not _G.SERVER_BLACKLIST[server_id] then
                    -- Для расширенного поиска позволяем более мягкие критерии
                    table.insert(all_servers, {
                        id = server_id,
                        players = server.playing,
                        ping = server.ping or 999
                    })
                end
            end
        end
    end
    
    -- Сортируем серверы по пингу (если доступен) и количеству игроков
    if #all_servers > 1 then
        table.sort(all_servers, function(a, b)
            -- Сначала по пингу
            if a.ping ~= b.ping then
                return a.ping < b.ping
            end
            -- Затем по количеству игроков (предпочитаем с большим количеством игроков)
            return a.players > b.players
        end)
    end
    
    -- Если нашли подходящие серверы
    if #all_servers > 0 then
        -- Выбираем наилучший сервер (первый в отсортированном списке)
        local chosen = all_servers[1]
        print("Найден подходящий сервер с " .. chosen.players .. " игроками: " .. chosen.id)
        
        -- ФИНАЛЬНАЯ ПРОВЕРКА перед прыжком
        if chosen.id == current_server then
            print("КРИТИЧЕСКАЯ ОШИБКА: пытаемся прыгнуть на текущий сервер! Отмена...")
            -- Пробуем другой сервер, если доступен
            if #all_servers > 1 then
                chosen = all_servers[2]
                print("Выбираем альтернативный сервер: " .. chosen.id)
            else
                -- Если других серверов нет, используем обычный телепорт
                print("Нет альтернативных серверов, используем обычный телепорт")
                game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
                is_server_hopping = false
                return
            end
        end
        
        -- Добавляем сервер в глобальную историю
        visited_servers[chosen.id] = os.time()
        saveServerHistory()
        
        -- Добавляем в глобальный блэклист
        _G.SERVER_BLACKLIST[chosen.id] = true
        
        -- Телепортируемся
        print("Телепортация на сервер: " .. chosen.id)
        local teleport_success = pcall(function()
            game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, chosen.id, game.Players.LocalPlayer)
        end)
        
        if not teleport_success then
            -- Если телепортация не удалась, пробуем обычный телепорт
            print("Ошибка телепортации, используем обычный телепорт")
            game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
        end
    else
        -- Если не нашли подходящие серверы, используем обычный телепорт
        print("Не найдено подходящих серверов, используем обычный телепорт")
        game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
    end
    
    -- Сбрасываем флаг через 10 секунд на случай сбоя телепортации
    task.spawn(function()
        task.wait(10)
        is_server_hopping = false
    end)
end

task.spawn(function() -- anti afk
    for _,v in pairs(getconnections(local_player.Idled)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocused)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocusReleased)) do v:Disable() end
end)

-- СИСТЕМА ОТПРАВКИ СООБЩЕНИЙ (ТОЛЬКО ПЕРВЫЕ 2)
task.spawn(function()
    -- Ждем немного, чтобы сначала скрипт проверил рифты
    task.wait(5)
    
    -- Проверяем, не отправляли ли мы уже сообщения
    if getgenv().ServerHopSettings.messages_sent then
        print("Сообщения уже были отправлены ранее")
        return
    end
    
    local channel
    local success = pcall(function()
        channel = game:GetService("TextChatService"):WaitForChild("TextChannels"):WaitForChild("RBXGeneral", 10)
    end)
    
    if not success or not channel then
        print("Failed to get chat channel, trying alternative method")
        -- Пробуем альтернативный метод для отправки сообщений
        pcall(function()
            local ChatEvents = game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
            if ChatEvents then
                local SayMessageRequest = ChatEvents:FindFirstChild("SayMessageRequest")
                if SayMessageRequest then
                    channel = {
                        SendAsync = function(_, message)
                            SayMessageRequest:FireServer(message, "All")
                            return true
                        end
                    }
                    success = true
                    print("Используем альтернативный метод чата")
                end
            end
        end)
    end
    
    if not success or not channel then
        print("Не удалось инициализировать систему чата, перезаходим без отправки сообщений")
        serverhop()
        return
    end
    
    print("Отправляем только 2 обязательных сообщения...")
    local messages_sent = 0
    
    -- Отправляем ТОЛЬКО два обязательных сообщения с интервалом в 3 секунды
    for i, message in ipairs(required_messages) do
        local sent = pcall(function()
            channel:SendAsync(message)
        end)
        
        if sent then
            messages_sent = messages_sent + 1
            print("Отправлено сообщение " .. i .. ": " .. message)
        else
            print("Не удалось отправить сообщение: " .. message)
        end
        
        -- Ждем 3 секунды между сообщениями
        task.wait(3)
    end
    
    -- Больше не отправляем дополнительных сообщений!
    
    print("Отправлено всего " .. messages_sent .. " сообщений из 2 запланированных")
    
    -- Устанавливаем флаг, что сообщения уже отправлены
    getgenv().ServerHopSettings.messages_sent = true
    
    -- Запускаем серверхоп после отправки сообщений
    print("Запуск серверхопа через 2 секунды...")
    task.wait(2)
    serverhop()
end)

do
    local rifts_found = 0 -- счетчик найденных рифтов
    local rifts_sent = 0  -- счетчик отправленных вебхуков
    
    for _, rift in pairs(rifts:GetChildren()) do
        if not rift:IsA("Model") or not whitelisted_eggs[rift.Name] then
            continue;
        end
        
        rifts_found = rifts_found + 1
        
        local data = whitelisted_eggs[rift.Name];
        local luck = "None";
        local gui
        
        -- Исправление: добавление проверок на nil и ожидания загрузки
        local display = rift:FindFirstChild("Display")
        if display then
            gui = display:FindFirstChildOfClass("SurfaceGui")
            if data.luck ~= nil then
                if gui == nil then
                    repeat 
                        task.wait(0.5)
                        gui = display:FindFirstChildOfClass("SurfaceGui")
                    until gui ~= nil or task.wait(5) -- таймаут 5 секунд на случай, если GUI не загрузится
                end
                
                if gui and gui:FindFirstChild("Icon") and gui.Icon:FindFirstChild("Luck") then
                    if not table.find(data.luck, gui.Icon.Luck.Text) then
                        continue
                    end
                    luck = gui.Icon.Luck.Text
                else
                    continue -- Пропускаем если нет нужных компонентов
                end
            end
        else
            continue -- Пропускаем если нет Display
        end
        
        if not gui or not gui:FindFirstChild("Timer") then
            continue -- Пропускаем если нет таймера
        end

        -- Создаем уникальный ID для рифта на основе его типа и координат (с округлением)
        local position = rift:GetPivot().Position
        local rift_id = string.format("%s_%d_%d_%d", 
            rift.Name, 
            math.floor(position.X), 
            math.floor(position.Y), 
            math.floor(position.Z))
        
        -- Проверяем, был ли этот рифт уже найден и отправлен ранее
        if rifts_cache[rift_id] then
            print("Rift already reported:", rift_id)
            continue
        end
        
        local time = os.time() + parse_time(gui.Timer.Text);
        local discord_time = time_to_discorddate(time);
        local height = math.floor(rift:GetPivot().Position.Y) - 4;
        
        -- ИСПРАВЛЕНО: Новый формат сообщения с кликабельной ссылкой и изображением справа
        local message = {
            content = data.role_id and data.role_id ~= "" and ("<@&" .. data.role_id .. ">") or "",
            embeds = {
                {
                    title = data.name .. " has been found!",
                    description = "**[🔗 Click here to join](" .. join_link .. ")**\n\n🪄Tutorial how to join pinned in https://discord.com/channels/1279491047616938005/1369315912049492069",
                    fields = {
                        {
                            name = "⏲ Expires in",
                            value = discord_time,
                            inline = true
                        },
                        {
                            name = "📏 Height",
                            value = tostring(height),
                            inline = true
                        },
                        {
                            name = "👤 Players",
                            value = tostring(#players:GetPlayers()),
                            inline = true
                        },
                        {
                            name = "🍀 Luck",
                            value = luck
                        }
                    },
                    color = data.color or 4508791, -- Используем цвет для рифта или стандартный
                    thumbnail = {
                        url = data.image
                    },
                    footer = {
                        text = "gg/easyrift • " .. os.date("%H:%M", os.time())
                    }
                }
            }
        }

        -- Сохраняем рифт в кэше с текущим временем
        rifts_cache[rift_id] = os.time()
        
        -- Сохраняем обновленный кэш
        pcall(function()
            writefile(cache_file, http_service:JSONEncode(rifts_cache))
        end)
        
        notify(data.webhook, message);
        rifts_sent = rifts_sent + 1
        
        -- Добавляем небольшую задержку между отправками вебхуков
        task.wait(0.5)
    end
    
    print("Rifts found:", rifts_found, "Webhooks sent:", rifts_sent)
    
    -- Не делаем моментальный серверхоп здесь, так как отправка сообщений запустит его
end

-- ГАРАНТИРОВАННЫЙ серверхоп спустя 15 секунд
task.spawn(function()
    task.wait(15) -- Ровно 15 секунд ожидания
    print("Гарантированный серверхоп после 15 секунд...")
    serverhop()
end)

-- РЕЗЕРВНЫЙ СЕРВЕРХОП спустя 30 секунд если что-то пошло не так
task.spawn(function()
    task.wait(30) -- 30 секунд
    print("Резервный серверхоп после 30 секунд...")
    game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
end)

-- Обработка кика с сервера
game:GetService("CoreGui"):FindFirstChild("RobloxPromptGui"):FindFirstChild("promptOverlay").ChildAdded:Connect(function(child) -- при кике игрой
    if child.Name == "ErrorPrompt" then
        print("Game kick detected, starting server hop")
        serverhop();
    end
end)

-- Проверяем, не тот же ли это сервер после телепортации
game:GetService("Players").PlayerAdded:Connect(function(player)
    if player == local_player then
        task.wait(3)
        if game.JobId == getgenv().ServerHopSettings.current_server_id then
            print("⚠️ ВНИМАНИЕ: Обнаружена повторная телепортация на тот же сервер! Повторяем серверхоп...")
            -- Добавляем сервер в глобальный блэклист и пытаемся телепортироваться снова
            _G.SERVER_BLACKLIST[game.JobId] = true
            task.wait(2)
            serverhop()
        else
            print("Успешная телепортация на новый сервер: " .. game.JobId)
            -- Обновляем текущий ID сервера
            getgenv().ServerHopSettings.current_server_id = game.JobId
        end
    end
end)

-- Выводим информацию в консоль при запуске
print("RiftFinder initialized successfully!")
print("Current server: " .. game.JobId)
print("Запоминаем сервера на " .. getgenv().ServerHopSettings.history_timeout .. " секунд")
print("Гарантированный серверхоп через 15 секунд")
