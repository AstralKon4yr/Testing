-- В самом начале скрипта добавляем защиту от слишком частых запусков:
if _G.SCRIPT_RUNNING_TIMESTAMP and os.time() - _G.SCRIPT_RUNNING_TIMESTAMP < 60 then
    print("⚠️ СКРИПТ УЖЕ ЗАПУЩЕН! АВАРИЙНАЯ ОСТАНОВКА...")
    _G.SCRIPT_RUNNING_TIMESTAMP = os.time()
    task.wait(5)
    game:GetService("TeleportService"):Teleport(85896571713843)
    return
end
_G.SCRIPT_RUNNING_TIMESTAMP = os.time()

-- [[ меняй эти настройки для оптимизации ]]
getgenv().Optimization = {
    state = true, -- вкл/выкл
    hide_screen = true, -- выключает прорисовку графики, бустит жесточайше фпс (вкл/выкл)
    fps = 10
};

-- Настройка серверхопа
getgenv().ServerHopSettings = {
    fast_mode = true, -- режим быстрого серверхопа
    hop_interval = 15, -- интервал серверхопа в секундах
    message_interval = 3, -- интервал между сообщениями в секундах
    messages_per_hop = 2 -- сколько сообщений отправлять на сервере
};

if getgenv().Optimization.state then
    if setfpscap ~= nil then setfpscap(getgenv().Optimization.fps); end
    UserSettings().GameSettings.MasterVolume = 0;
    settings().Rendering.QualityLevel = "Level01";
    game:GetService("RunService"):Set3dRenderingEnabled(not getgenv().Optimization.hide_screen);
end

local players = game:GetService("Players");
local local_player = players.LocalPlayer;
local http_service = game:GetService("HttpService");
local teleport_service = game:GetService("TeleportService");

-- НОВЫЕ СООБЩЕНИЯ ДЛЯ ОТПРАВКИ
local chat_messages = {
    "Best Grow A Garden pet & sheckles shop ģģ/ astral-market",
    "Sheckles shop ģģ/ astral-market",
    "Beat the game with ģģ/ astral-market",
    "Giveaways happening at ģģ/ astral-market"
};

-- Защита от AFK кика
task.spawn(function()
    for _,v in pairs(getconnections(local_player.Idled)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocused)) do v:Disable() end
    for _,v in pairs(getconnections(game:GetService("UserInputService").WindowFocusReleased)) do v:Disable() end
end)

-- Функция отправки сообщений (поддержка старой и новой системы чата)
local function sendMessage(message)
    local success = false
    
    -- Попытка 1: Использование TextChatService (новая система)
    pcall(function()
        local TextChatService = game:GetService("TextChatService")
        if TextChatService then
            local channel = TextChatService:FindFirstChild("TextChannels")
            if channel then
                local general = channel:FindFirstChild("RBXGeneral")
                if general then
                    general:SendAsync(message)
                    print("📝 Отправлено через TextChatService: " .. message)
                    success = true
                end
            end
        end
    end)
    
    -- Попытка 2: Старая система чата, если новая не сработала
    if not success then
        pcall(function()
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local ChatEvents = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            if ChatEvents then
                local SayMessageRequest = ChatEvents:FindFirstChild("SayMessageRequest")
                if SayMessageRequest then
                    SayMessageRequest:FireServer(message, "All")
                    print("📝 Отправлено через Legacy Chat: " .. message)
                    success = true
                end
            end
        end)
    end
    
    -- Попытка 3: Прямой метод для некоторых игр
    if not success then
        pcall(function()
            game:GetService("Players"):Chat(message)
            print("📝 Отправлено через Players:Chat: " .. message)
            success = true
        end)
    end
    
    return success
end

-- УПРОЩЕННАЯ И НАДЕЖНАЯ СИСТЕМА СЕРВЕРХОПА
local function serverHop()
    print("🔄 Начинаем серверхоп...")
    
    -- Анкорим персонажа для предотвращения крашей
    pcall(function()
        if local_player.Character and local_player.Character:FindFirstChild("HumanoidRootPart") then
            local_player.Character.HumanoidRootPart.Anchored = true
        end
    end)
    
    -- Целевая игра (Bubble Gum Simulator)
    local placeId = 85896571713843
    
    -- Сначала пробуем найти случайный сервер
    local serversUrl = string.format(
        "https://games.roblox.com/v1/games/%s/servers/Public?sortOrder=Asc&limit=100", 
        placeId
    )
    
    local servers
    pcall(function()
        servers = http_service:JSONDecode(game:HttpGet(serversUrl)).data
    end)
    
    -- Если нашли серверы, выбираем подходящий
    if servers and #servers > 0 then
        -- Фильтруем серверы (не заходим на текущий)
        local filteredServers = {}
        for _, server in ipairs(servers) do
            if server.playing < 49 and server.id ~= game.JobId then
                table.insert(filteredServers, server)
            end
        end
        
        -- Если остались подходящие серверы
        if #filteredServers > 0 then
            -- Выбираем случайный сервер
            local selectedServer = filteredServers[math.random(1, #filteredServers)]
            print("🎯 Найден сервер с " .. selectedServer.playing .. " игроками. ID: " .. selectedServer.id)
            
            -- Телепортируемся
            teleport_service:TeleportToPlaceInstance(placeId, selectedServer.id, local_player)
            print("✈️ Телепортация на сервер...")
            return true
        end
    end
    
    -- Если не нашли подходящие серверы или произошла ошибка, просто телепортируемся в игру
    print("⚠️ Не удалось найти подходящие серверы. Телепортация напрямую...")
    teleport_service:Teleport(placeId, local_player)
    return true
end

-- НОВАЯ УСОВЕРШЕНСТВОВАННАЯ СИСТЕМА СПАМА
-- Отдельный поток для отправки сообщений
task.spawn(function()
    -- Даем игре время загрузиться
    task.wait(3)
    
    print("📣 Запущена система спама сообщений")
    local messages_sent = 0
    local last_message = ""
    
    while true do
        -- Выбираем случайное сообщение (не повторяя предыдущее)
        local message
        repeat
            message = chat_messages[math.random(1, #chat_messages)]
        until message ~= last_message or #chat_messages <= 1
        
        last_message = message
        
        -- Отправляем сообщение
        local success = sendMessage(message)
        
        if success then
            messages_sent = messages_sent + 1
            print("📨 Отправлено сообщение #" .. messages_sent .. ": " .. message)
        end
        
        -- Если отправили достаточно сообщений, переходим на новый сервер
        if messages_sent >= ServerHopSettings.messages_per_hop then
            print("🔄 Отправлено " .. messages_sent .. " сообщений. Начинаем серверхоп...")
            task.wait(1) -- Небольшая пауза перед хопом
            serverHop()
            break -- Останавливаем цикл, так как будем переходить на другой сервер
        end
        
        -- Ожидание перед следующим сообщением
        task.wait(ServerHopSettings.message_interval)
    end
end)

-- РЕЗЕРВНАЯ СИСТЕМА СЕРВЕРХОПА
-- Таймер для перехода на новый сервер, если что-то пошло не так
task.spawn(function()
    -- Ждем максимальный таймаут на сервере
    local max_time = ServerHopSettings.hop_interval
    print("⏱️ Максимальное время на сервере: " .. max_time .. " секунд")
    
    task.wait(max_time)
    
    print("⏰ Таймаут истек! Принудительный серверхоп...")
    serverHop()
end)

-- Обработка кика с сервера
game:GetService("CoreGui"):FindFirstChild("RobloxPromptGui"):FindFirstChild("promptOverlay").ChildAdded:Connect(function(child)
    if child.Name == "ErrorPrompt" then
        print("❌ Обнаружен кик с сервера. Переходим на новый сервер...")
        serverHop()
    end
end)

-- Обработка ошибок телепортации
local_player.OnTeleport:Connect(function(state)
    if state == Enum.TeleportState.Failed then
        print("❌ Телепортация не удалась! Повторная попытка...")
        task.wait(3)
        serverHop()
    end
end)

print("✅ Скрипт успешно запущен!")
